import { Provider } from '@angular/core';
import { URGENTLY_PRELOADABLE_CATEGORY } from './urgently-preloadable';


export const PRELOADABLE_CATEGORY_CONFIG_PROVIDERS: Provider[] = [
    URGENTLY_PRELOADABLE_CATEGORY,
];
